export type JobRoleRequest = {
    roleName: string,
    location: string,
    capability: string,
    band: string,
    closingDate: Date,
    description: string,
    responsibilities: string,
    jobSpec: string
}