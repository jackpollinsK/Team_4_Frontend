export type JobRoleRequest = {
    roleName: string,
    location: number,
    capability: number,
    band: number,
    closingDate: Date,
    description: string,
    responsibilities: string,
    jobSpec: string
}