import { expect } from 'chai';
import sinon from 'sinon';
import express from "express";

describe('JobRoleController', function () {
    afterEach(() => {
        sinon.restore();
    });

    describe('getPropmtForm', function () {
        //Check if user is logged in

        //Check if user isn't logged in

    });

    describe('postPropmtForm', function () {
        //Check if post form is successful

        //Check if Open AI API returns an error

        //Check if getAllJobs returns a 500

        //Check result when no results are found

    });
});